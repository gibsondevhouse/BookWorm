# Tests

This directory now contains the small integration suite for the Phase 0 Character draft, release, and public-read slice.

- `phase0CharacterSlice.test.ts` exercises the session-backed admin flow and verifies that public reads stay release-bound.
